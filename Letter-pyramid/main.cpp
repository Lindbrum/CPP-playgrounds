#include <iostream>

using namespace std;

int main()
{
    //Take the user input
    string pyramid_base {};
    cout << "Input a string to build the pyramid on: ";
    cin >> pyramid_base;

    //Build the pyramid from top to bottom
    for (size_t i = 0; i <)

    return 0;
}
